{
  "id": "card-ex-filter",
  "meta": {
    "title": "Filters",
    "tags": [],
    "desc": "Interactive filter controls — user selects region and product"
  },
  "requires": ["orders"],
  "provides": [{ "bindTo": "selections", "src": "state.fieldValues" }],
  "compute": [
    { "bindTo": "regions",  "expr": "$sort($distinct(requires.orders.region))" },
    { "bindTo": "products", "expr": "$sort($distinct(requires.orders.product))" }
  ],
  "view": {
    "elements": [
      {
        "kind": "filter",
        "data": {
          "bind": "computed_values",
          "writeTo": "state.fieldValues",
          "fields": {
            "type": "object",
            "properties": {
              "region":  { "type": "string", "title": "Region" },
              "product": { "type": "string", "title": "Product" }
            }
          }
        }
      }
    ],
    "layout": { "board": { "col": 4, "order": 3 }, "canvas": { "x": 50, "y": 320, "w": 220, "h": 160 } }
  },
  "state": {
    "fieldValues": {}
  }
}
