rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Board data — allow read/write from authenticated Cloud Functions only.
    // Client SDK access is not used; all requests go through the Cloud Function.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
